import { CreatePokemonContainer } from "./StyledCreatePokemon";

const CreatePokemon = () => {
  return (
    <CreatePokemonContainer>
      <button>create pokemon</button>
    </CreatePokemonContainer>
  );
};

export default CreatePokemon;
